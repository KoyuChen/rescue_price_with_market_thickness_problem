% New pages 3--8 for the group-meeting note.
% The final PDF physically prepends pages 1--2 of group_meeting_numerics.pdf.
\documentclass[11pt]{article}
\usepackage[letterpaper,margin=2.05cm]{geometry}
\usepackage{amsmath,amssymb}
\usepackage{booktabs,array,tabularx,colortbl}
\usepackage{graphicx,xcolor}
\usepackage{float,enumitem,microtype,multicol}
\usepackage[font=small,skip=3pt]{caption}
\usepackage{titlesec}
\titlespacing*{\section}{0pt}{5pt plus 2pt}{3pt}
\titlespacing*{\subsection}{0pt}{5pt plus 2pt}{2pt}
\setlength{\abovedisplayskip}{4pt}
\setlength{\belowdisplayskip}{4pt}
\setlength{\textfloatsep}{5pt}
\setlist[itemize]{leftmargin=*,topsep=2pt,itemsep=1pt,parsep=0pt}
\setlist[enumerate]{leftmargin=*,topsep=2pt,itemsep=2pt,parsep=0pt}
\newcolumntype{L}[1]{>{\raggedright\arraybackslash}p{#1}}
\newcolumntype{Y}{>{\raggedright\arraybackslash}X}
\definecolor{basegray}{HTML}{6B7280}
\definecolor{fixedorange}{HTML}{D55E00}
\definecolor{searchblue}{HTML}{0072B2}
\definecolor{softgray}{HTML}{F3F4F6}
\definecolor{softorange}{HTML}{FFF1E8}
\definecolor{softblue}{HTML}{EAF4FA}
\newcommand{\takeaway}[1]{\par\smallskip\noindent\textbf{Takeaway.} #1\par\smallskip}
\newcommand{\E}{\mathbb E}
\newcommand{\WPBE}{\mathrm{WPBE}}

\begin{document}
\setcounter{page}{3}
\setcounter{section}{1}
\setcounter{figure}{2}

\section{Why the design becomes $(p_1,p_2,s)$}

\takeaway{Pages 1--2 isolate same-order strategic waiting in the exact
one-driver benchmark.  The market-design comparison now adds a fresh Poisson
cohort in every second window and asks whether the platform should reprice the
same catchment, widen the catchment, or do both.}

\subsection{Three strictly nested mechanism classes}

\begin{center}
\small
\renewcommand{\arraystretch}{1.17}
\begin{tabularx}{.995\textwidth}{@{}L{2.35cm}L{2.6cm}Y Y@{}}
\toprule
Mechanism class & Commitment & Second-window core & What is added?\\
\midrule
\rowcolor{softgray}
Flat fixed reach $\mathcal M_0$ & $(p,p,1)$ &
$N_2^C\sim\mathrm{Pois}(m)$ & No contingent price and no outer search.\\
\rowcolor{softorange}
Fixed-reach rescue $\mathcal M_F$ & $(p_1,p_2,1)$ &
$N_2^C\sim\mathrm{Pois}(m)$ & A committed rescue price in the same catchment.\\
\rowcolor{softblue}
Expanded-search rescue $\mathcal M_E$ & $(p_1,p_2,s)$, $s\ge1$ &
$N_2^C\sim\mathrm{Pois}(m)$ & An independent outer annulus plus the same rescue price.\\
\bottomrule
\end{tabularx}
\end{center}

Thus ``fixed reach'' fixes the notification geography, not driver identities.
Both rescue mechanisms contain time-homogeneous fresh arrivals in period 2.
Their feasible sets are
\[
\mathcal M_0=\{(p,p,1)\}
\subset
\mathcal M_F=\{(p_1,p_2,1):p_2\ge p_1\}
\subset
\mathcal M_E=\{(p_1,p_2,s):p_2\ge p_1,\ 1\le s\le\bar s\}.
\tag{5}
\]
The flat benchmark has one optimized price; it is not a fixed-$p_1$ policy.

\subsection{$s$ is an area multiplier, not a participation share}

Let spatially homogeneous fresh drivers form a Poisson point process.  Normalize
the core catchment to area one and let its radius be $R_0$.  Committing to
multiplier $s$ means
\[
R(s)=R_0\sqrt{s},\qquad
N_2^O(s)\sim\mathrm{Pois}((s-1)m),\qquad
N_2^C\perp N_2^O(s).
\tag{6}
\]
Hence the expanded pool is $\mathrm{Pois}(sm)$ and the core is its Poisson
thinning with retention probability $1/s$.  At $s=4$, for example, area is
quadrupled but radius only doubles.

\subsection{The object plotted below}

For market environment $\theta=(m,\beta,\delta)$, each line reports a separate
equilibrium-constrained optimum
\[
V_k(\theta)=\max_{\mu\in\mathcal M_k}
\min_{a\in\mathcal E^{\WPBE}(\mu;\theta)}M(\mu,a),
\qquad k\in\{0,F,E\}.
\tag{7}
\]
The nesting makes $V_E\ge V_F\ge V_0$; the substantive question is where each
increment is large after all permitted controls are re-optimized.

\clearpage
\section{Inner cutoff-WPBE, then outer mechanism design}

\takeaway{The platform commits first.  For every candidate mechanism, driver
strategies, rider continuation, and Bayesian beliefs are solved as a complete
cutoff-WPBE.  Only then does the platform compare mechanisms.}

\begingroup\small

\subsection{Information and timing for the same focal order}

\begin{center}
\footnotesize
\renewcommand{\arraystretch}{1.04}
\begin{tabularx}{.995\textwidth}{@{}L{1.15cm}Y L{4.2cm}@{}}
\toprule
Stage & Action & Information\\
\midrule
0 & Platform commits to $\mu=(p_1,p_2,s)$; a rider of value $v$ posts iff
$v\ge p_1$. & $m,\beta,\delta$, the cost laws, and $\mu$ are public.\\
1 & $N_1\sim\mathrm{Pois}(m)$ incumbents see this order and choose accept
$p_1$ or reject-and-wait. & Each sees the order, both prices, $s$, and own
persistent cost $c$; counts and other costs are hidden.\\
2 & After universal rejection, the rider chooses abandon, repeat at $p_1$,
or rescue at $p_2$. & The rider sees failure but no driver count or future
fresh responses; beliefs are induced by the equilibrium cutoff.\\
3 & The chosen terminal notification is sent; fresh drivers see the order for
the first time and may respond. & Rejectors and fresh volunteers enter one
anonymous lottery.  A rejector may first exit or lose eligibility; pickup cost
is paid only by the selected driver.\\
\bottomrule
\end{tabularx}
\end{center}

\subsection{Winner-only pickup cost and terminal coverage}

Write area rank as $u=(r/R_0)^2$.  Core pickup is absorbed into $c\sim U[0,1]$;
the extra cost from expansion is
\[
d(u)=\tau(\sqrt u-1)_+.
\]
A fresh driver's response payoff is
$h(\lambda)[p-c-d(u)]$, where
$h(z)=(1-e^{-z})/z$.  Because the same assignment probability multiplies
payment and pickup cost,
\[
\text{respond}\iff c+d(u)\le p,\qquad
e(p,s)=m\int_0^s F\!\left(p-d(u)\right)du.
\tag{8}
\]
There is no sunk relocation cost and no fresh-entry fixed point.  Let a
rejector remain available with probability $\alpha$ and remain eligible
conditional on availability with probability $\chi$; write
$\omega=\alpha\chi$.  Given cutoff $a$, let $s_1=1,s_2=s$ and
\[
I_j^\omega(a)=\omega m[F(p_j)-F(a)]_+,\quad
\lambda_j(a)=I_j^\omega(a)+e(p_j,s_j),\quad C_j(a)=1-e^{-\lambda_j(a)}.
\tag{9}
\]
The rider compares $0$, $C_1(\beta v-p_1)$, and $C_2(\beta v-p_2)$; let the
resulting conditional action masses be $\eta_0(a),\eta_1(a),\eta_2(a)$.

\subsection{Strict cutoff-WPBE test and the outer problem}

For a proposed aggregate cutoff, a type-$c$ incumbent's accept-minus-wait
payoff is
\[
D(c;a)=h(mF(a))(p_1-c)-\delta e^{-mF(a)}
\omega\sum_{j=1}^2\eta_j(a)h(\lambda_j(a))(p_j-c)_+.
\tag{10}
\]
Together with the rider and fresh-driver strategies above, $a$ is a cutoff-WPBE
iff beliefs obey Bayes' rule and
\[
D(c;a)\ge0\ \forall c<a,\qquad D(c;a)\le0\ \forall c>a,
\tag{11}
\]
with indifference at an interior cutoff.  Since $D$ is piecewise affine with
kinks only at $p_1,p_2$, the numerics verify (11) exactly at interval endpoints,
not on a sampled type grid.  The computation respects the nesting
\[
\text{fix }p_1\ \longrightarrow\
\max_{p_2,s}\min_{a\in\mathcal E^{\WPBE}}M
\ \longrightarrow\ \max_{p_1},
\tag{12}
\]
and enumerates the entire cutoff correspondence before conservative selection.
The archived curves set $\omega=1$; lower retention is an explicit primitive.
\endgroup

\clearpage
\section{Part 2: fixed-reach rescue}

\takeaway{Fixed-reach rescue reprices the second window without changing its
geography.  It still has a new core Poisson cohort; its distinctive tool is
the committed wedge $p_2-p_1$, not the existence of fresh arrivals.}

\begin{figure}[H]
\centering
\includegraphics[width=.99\textwidth]{../figures/figure_fixed_rescue_story.pdf}
\caption{Fixed-reach rescue versus the flat two-window benchmark.  Every point
first solves the complete cutoff-WPBE and then re-optimizes the permitted
mechanism.  Right: second-window willing-driver intensity under the optimized
fixed-rescue policy.}
\label{fig:fixed-story}
\end{figure}

Under repeat, the terminal pool contains screened incumbents
$\omega m[F(p_1)-F(a)]_+$ and core fresh volunteers $e(p_1,1)$.  Under rescue,
it contains $\omega m[F(p_2)-F(a)]_+$ and $e(p_2,1)$.  For uniform costs,
\[
e(p,1)=mp.
\tag{13}
\]
Thus increasing $p_2$ works through two supply channels, but it also increases
the continuation value of rejecting the focal order and may lower $a$.

At $(\beta,\delta,\tau)=(.8,.8,.25)$, the optimized fixed-rescue increment is
only $0.65$ percentage points at $m=.5$, peaks at $2.81$ points near $m=6$,
and remains $2.24$ points at $m=16$.  This hump is an equilibrium-design
pattern: each orange point has its own $(p_1^*,p_2^*)$ and its own WPBE cutoff.

\noindent\textbf{Interpretation.}
In a very thin market, repricing a small catchment has little supply to work
with.  As $m$ grows, the second-window core cohort and screened backup pool
make contingent pay more useful.  In a very thick market, the flat mechanism
already completes most requests, limiting the remaining gain.

\clearpage
\section{Part 3: expanded-search rescue}

\takeaway{Expanded search preserves the core cohort and adds only the outer
annulus.  Distant drivers volunteer less often because the assigned winner,
and only the winner, pays the incremental pickup cost.}

\begin{figure}[H]
\centering
\includegraphics[width=.99\textwidth]{../figures/figure_expanded_search_story.pdf}
\caption{Incremental value and supply composition of expanded search.  Each
blue point re-optimizes $(p_1,p_2,s)$ outside its complete cutoff-WPBE; the
orange comparator separately re-optimizes $(p_1,p_2,1)$.}
\label{fig:expanded-story}
\end{figure}

For $F=U[0,1]$, $p\in[0,1]$, and $\tau>0$, define
\[
R(p,s)=\min\{\sqrt{s},1+p/\tau\}.
\]
Poisson thinning gives the closed-form fresh volunteer intensity
\[
e(p,s)=mp+m(p+\tau)(R^2-1)
-\frac{2m\tau}{3}(R^3-1).
\tag{14}
\]
Its marginal outer supply is
\[
\frac{\partial e}{\partial s}
=m[p-\tau(\sqrt{s}-1)]_+,\qquad
s^{\mathrm{sat}}(p)=(1+p/\tau)^2.
\tag{15}
\]
Thus pickup friction creates a physical response saturation.  It does not make
notifications economically free.  If expanded rescue is executed with
ex-ante probability $q_R$, expected extra contacts are
\[
Q^O=q_Rm(s-1),\qquad J_\kappa=BM-\kappa Q^O.
\tag{16}
\]
Completion with $\kappa=0$ is a maximal-completion frontier; an economic scope
optimum requires $\kappa>0$ or an equivalent notification budget.

At $(\beta,\delta,\tau)=(.8,.8,.25)$, the incremental value of expanded search
over optimized fixed rescue peaks at $12.64$ percentage points near $m=1$ and
falls to $1.42$ points at $m=16$.  The optimized multiplier declines from its
cap $s^*=4$ in thin markets to $s^*=2.09$ at $m=16$.

\noindent\textbf{Interpretation.}
Price chooses which reached drivers are willing; $s$ chooses who can first
learn about the failed order.  With free contacts, $s^*$ may hit $\bar s$ or
the physical saturation set.  With $\kappa>0$, search expands only while its
marginal completion value exceeds the opportunity cost of another expected
contact.

\clearpage
\section{The two controls peak in different thickness regions}

\takeaway{In the fully optimized cutoff-WPBE numerics, expanded search creates
its largest incremental gain first; contingent pricing reaches its maximum
later.  The right panel gives the common-branch theorem that explains this
ordering.  The left panel is evidence for the re-optimized model, not its proof.}

\begin{figure}[H]
\centering
\includegraphics[width=.995\textwidth]{../figures/figure5_ordered_peaks.pdf}
\caption{Left: every point separately re-solves the inner cutoff-WPBE and
re-optimizes all controls available in that mechanism class.  Right: one
illustrative set of effective rates for the closed-form theorem; the ordering
holds for every $0\le\lambda_0<\lambda_F<\lambda_E$ with
$r+\lambda_0>0$.}
\label{fig:ordered-peaks}
\end{figure}

\begin{center}
\small
\renewcommand{\arraystretch}{1.12}
\begin{tabularx}{.98\textwidth}{@{}L{2.25cm}Y Y@{}}
\toprule
Thickness regime & Expanded-search layer $V_E-V_F$ & Price layer $V_F-V_0$\\
\midrule
Scarce & Geographic reach is binding; the gain peaks near $m=1$ in the main
slice. & Repricing a small core acts on little supply.\\
Intermediate & Its marginal value falls as the core itself becomes reliable. &
The screened and core-fresh backup pools are now large enough for contingent
pay to peak near $m=6$.\\
Ample & Added outer coverage has little room to improve completion. & The flat
optimized mechanism also becomes reliable, so the price increment declines.\\
\bottomrule
\end{tabularx}
\end{center}

\noindent\colorbox{softgray}{\parbox{.965\textwidth}{\small
\textbf{Exact status.}  The numerical peak locations use
$(\beta,\delta,\tau,\omega)=(.8,.8,.25,1)$.  Af\`eche--Liu--Maglaras-style
``scarce--intermediate--ample'' language is appropriate for these regimes;
one should not call the re-optimized value envelopes globally single-peaked
until branch switching and rider composition are controlled.}}

\clearpage
\section{A benchmark theorem, a full-model target, and the boundary}

\takeaway{The strict peak order is proved on a common Poisson-WPBE branch.
For the fully re-optimized mechanism, the honest theorem target is an ordering
of argmax sets under an explicit branch-stability condition.}

\begingroup\small
\noindent\colorbox{softblue}{\parbox{.965\textwidth}{
\textbf{Theorem (ordered peaks of nested rescue controls).}
Fix one active cutoff-WPBE and rider-continuation branch.  Suppose first-window
failure contributes $e^{-rm}$ and the nested terminal mechanisms induce
$0\le\lambda_0<\lambda_F<\lambda_E$, with $r+\lambda_0>0$.  Then
\[
\Delta_P^0=e^{-rm}(e^{-\lambda_0m}-e^{-\lambda_Fm}),\qquad
\Delta_S^0=e^{-rm}(e^{-\lambda_Fm}-e^{-\lambda_Em})
\]
are strictly single-peaked, with
\[
m_P^*=\frac{\log[(r+\lambda_F)/(r+\lambda_0)]}{\lambda_F-\lambda_0},\qquad
m_S^*=\frac{\log[(r+\lambda_E)/(r+\lambda_F)]}{\lambda_E-\lambda_F},
\]
and
\[
\boxed{\ m_S^*<1/(r+\lambda_F)<m_P^*\ }.
\tag{17}
\]}}

\begin{multicols}{2}
\subsection{Proof and full-model lift}

For $g_{a,b}(m)=e^{-am}-e^{-bm}$, $0<a<b$, the derivative changes sign once
and
\[
m^*(a,b)=\frac{\log(b/a)}{b-a},\qquad \frac1b<m^*(a,b)<\frac1a.
\]
Apply this to the adjacent pairs
$(r+\lambda_0,r+\lambda_F)$ and
$(r+\lambda_F,r+\lambda_E)$.

For the optimized model define
\[
\mathcal S=\arg\max_m(V_E-V_F),\qquad
\mathcal P=\arg\max_m(V_F-V_0).
\]
The desired statement is
$\sup\mathcal S<\inf\mathcal P$.  A sufficient condition is that both optimized
gaps remain uniformly within half of their benchmark separation margins at
$\bar m=1/(r+\lambda_F)$.  This condition permits multiple optimal policies;
it does not assume differentiable envelopes.

\subsection{Adversarial boundary}

The common branch is essential.  Let
$\Delta_P=e^{-m}-e^{-2m}$ and multiply the search gain by the
mechanism-specific continuation factor
$\eta(m)=(1-e^{-m})^2$.  Then
\[
m_P^*=\log2,\qquad m_S^*=\log(5/2)>m_P^*.
\]
Both gains remain smooth and uniquely peaked.  Rider-composition changes alone
can reverse their order; policy switching can additionally create multiple
peaks.

\subsection{Closest theorem styles}

\begin{itemize}
\item Af\`eche--Liu--Maglaras: scarce/moderate/ample thresholds and
zero--positive--zero control value, not a unique peak.
\item Zhao--Papier--Teo: quasi-convex thickness cost and an intermediate
optimum; weak quasi-convexity does not imply uniqueness.
\item Wang--Zhang--Zhang: characterize search scope by a scalar index crossing.
\item Here: order the peaks of two \emph{adjacent nested controls} after the
inner cutoff-WPBE is made explicit.
\end{itemize}
\end{multicols}

\vspace{-3pt}
\noindent\colorbox{softgray}{\parbox{.965\textwidth}{\small
\textbf{Economic closure for search.}
On a common branch, let added scope create willing rate $\sigma(s)$ and contact
rate $n(s)$.  Under $J_\kappa=BM-\kappa Q^O$, an interior scope satisfies
\[
\Psi(s;m)=
\frac{B e^{-[\lambda_F+\sigma(s)]m}\sigma'(s)}
{\kappa n'(s)}=1.
\]
Expand for $\Psi>1$ and contract for $\Psi<1$.  With $\kappa=0$, the solution
is governed by the search cap or physical saturation, so it should be reported
as the maximal-completion benchmark.}}
\endgroup

\paragraph{References.}\footnotesize
Af\`eche, Liu, Maglaras (2023), \emph{M\&SOM} 25(5), 1890--1908.
Zhao, Papier, Teo (2024), \emph{M\&SOM}.
Hu, Hu, Zhu (2022), \emph{M\&SOM} 24(1), 91--109.
Wang, Zhang, Zhang (2024), \emph{Operations Research} 72(3), 1278--1297.

\end{document}
