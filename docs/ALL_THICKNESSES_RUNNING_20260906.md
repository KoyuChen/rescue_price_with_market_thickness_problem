# 六个厚度运行记录

2026-09-06 07:06 香港时间核验：m=1、3、6、12、24、48 均有实际运行的求解进程，
检查点持续推进。新增通用入口 `run_accelerated_price_search.py`，补齐 m=1 的外层流程。
这是一次运行状态快照，不是六个厚度的优化结果。

| 厚度 | 运行目录 | 当前步骤 |
|---:|---|---|
| 1 | `runs/m1_outer_search_20260906` | 新启动；验证外层流程的固定菜单 |
| 3 | `runs/m3_outer_search_20260906` | 继续；验证外层流程的固定菜单 |
| 6、12、24、48 | `runs/thick_checkpointed_20260905` | 从已保存策略恢复后继续固定菜单验证 |

机器可读快照见 [all_thicknesses_status.json](../results/all_thicknesses_20260906/status.json)。
记录中的迭代数只表示该菜单已提交的策略更新次数，不能作为整体搜索完成百分比。

六路均先求解并独立审计测试菜单，通过后自动评估 rescue 全域 0.05 网格和
flat 全域 0.0025 网格（合计 611 个不同菜单），再逐步加密 rescue 候选至 0.0025。
任何验证失败都会保留结果并阻止该厚度进入后续搜索，不能将失败当作通过。

精度未降低：2048 个成本点；m=1、3 使用 24 个路线点，其余四路使用 48 个。
每菜单 50000 训练计数样本、两组各 1000000 独立审计样本、64 节点并列分配积分；
候选选择 200 万市场，冻结报告 1000 万独立配对市场。

m=1、3 使用已验证的等价向量化和缓存核，其余四路保留启动时的求解源码。
所有菜单按相同的 homotopy 规则冷启动；已有固定菜单的温启动精修结果仅用作
启动门槛依据，不直接代替外层流程的均衡分支选择。每次策略更新原子保存检查点；
相同命令重启可恢复，输入或源码哈希不符则拒绝恢复。

新增 m=1 命令（从仓库根目录运行，已发布输入位于 `results/`）：

```bash
OPENBLAS_NUM_THREADS=1 OMP_NUM_THREADS=1 python -u run_accelerated_price_search.py --m 1 --gate-root results/research_refined_strict_20260905 --model-request results/research_gate_20260905/request.json --output runs/m1_outer_search
```

继续已有运行时应使用原来的输入和输出路径，避免创建新的冷启动任务。
核验方式为检查实际进程 CPU 时间及更新中的策略检查点，不能只凭
`supervisor.json` 中的 `running` 字段认定任务仍存活。

此前已完成的 m=1、3 固定菜单比较继续有效，但尚无本轮优化后的 V*(m)。
连续类型稳定性、路线加密与最终价格加密确认仍未完成。
